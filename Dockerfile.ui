FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1
ENV PYTHONPATH=/app

WORKDIR /app

RUN apt-get update \
    && apt-get install -y --no-install-recommends build-essential libpq-dev curl \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml poetry.lock ./
RUN pip install --upgrade pip "poetry==1.8.4" poetry-plugin-export \
    && poetry export --with ui --without-hashes --format requirements.txt --output requirements.txt \
    && pip install --no-cache-dir -r requirements.txt

COPY ui ./ui
COPY README.md ./

EXPOSE 8500

CMD ["streamlit", "run", "ui/main.py", "--server.port=8500", "--server.address=0.0.0.0"]

